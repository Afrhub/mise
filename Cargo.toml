[package]
name = "mise-swarm"
version = "0.1.0"
edition = "2021"
description = "Swarm topology initialization and agent coordination"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
